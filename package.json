{
  "name": "tiebreak",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "workspaces": [
    "client",
    "server",
    "shared"
  ],
  "scripts": {
    "dev": "concurrently -n client,server -c cyan,magenta \"yarn workspace @tiebreak/client dev\" \"yarn workspace @tiebreak/server dev\"",
    "dev:client": "yarn workspace @tiebreak/client dev",
    "dev:server": "yarn workspace @tiebreak/server dev",
    "build": "yarn workspace @tiebreak/client build && yarn workspace @tiebreak/server build",
    "lint": "yarn workspace @tiebreak/client lint && yarn workspace @tiebreak/server lint"
  },
  "devDependencies": {
    "@neon/config": "^1.3.2",
    "@neon/env": "^1.2.3",
    "concurrently": "^9.1.0"
  }
}
